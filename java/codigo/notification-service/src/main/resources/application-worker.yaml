server:
  port: ${WORKER_HEALTH_PORT:8081}

spring:
  application:
    name: notification-worker
  rabbitmq:
    addresses: ${NOTIFICATION_AMQP_URL}
    listener:
      simple:
        acknowledge-mode: manual
        default-requeue-rejected: false
        observation-enabled: true
    template:
      observation-enabled: false
